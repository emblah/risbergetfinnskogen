{
  "name": "risbergetfinnskogen.no",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "eleventy --serve --output=_site",
    "build": "eleventy --pathprefix=risbergetfinnskogen && npm run check",
    "check": "node scripts/check-site.js --site-root docs --path-prefix risbergetfinnskogen"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.6",
    "parse5": "8.0.0"
  }
}
